streamlit==1.40.0
pandas>=2.2.0,<3.0.0
plotly>=5.15.0,<6.0.0
altair>=5.0.0
matplotlib>=3.6.0,<4.0.0
numpy>=1.22.0,<2.0.0
cProfile==0.8.3  # cProfile is built-in and doesn't need installation, but you can specify its version if needed.
